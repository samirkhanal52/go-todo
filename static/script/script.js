// array to hold the todo list
let tododItems = []

function addTodo(text) {
    const todo = {
        title,
        checked: false,
        description,
    };

    tododItems.push(todo)
    console.log(tododItems);
}

const form = document.querySelector('.js-form');
